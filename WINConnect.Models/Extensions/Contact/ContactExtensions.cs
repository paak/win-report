using System;
using System.Collections.Generic;
using System.Linq;
using WINConnect.Models;

namespace WINConnect.Models.Extensions.Contact
{
    public static class ContactExtensions
    {
        /*
        public static Nullable<DateTime> GetLatestLogin(this ICollection<Contact_Login> history)
        {
            if (history == null)
            {
                return null;
            }
            Contact_Login login = history.OrderByDescending(x => x.LoggedOn).FirstOrDefault();

            if (login == null)
            {
                return null;
            }
            return login.LoggedOn;
        }
         * */
    }
}