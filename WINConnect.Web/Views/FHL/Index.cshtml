@model IEnumerable<WINConnect.Models.FWBLog>
@{
    Layout = null;
    ViewBag.Title = "FHL History";
}

@if (Model != null)
{
    <table class="table table-striped table-condensed">
        <thead>
            <tr>
                <th data-sort="">Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SentOn</td>
                </tr>
            }
        </tbody>
    </table>
}